# dns-linux-simulation
This project is a functional simulation of a Domain Name System (DNS) infrastructure designed for a Linux environment. It replicates the interaction between a Recursive Resolver and an Authoritative Name Server to demonstrate how hostnames (like www.cisco.test) are translated into machine-readable IP addresses.
